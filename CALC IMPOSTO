<html>
	<head>
		<meta charset="utf-8">
		<title>CALCULAR VALOR IMPOSTO</title>

	</head>

	<body>

		<?php


		/*$salario = 7000;
		$percentagem_renda = 0;

		if($salario <= 1903.98) {
			$percentagem_renda = 0;
		} else if ($salario >= 1903.99 && $salario <= 2826.65) {
			$percentagem_renda = 0.075;
		} else if ($salario >= 2826.66 && $salario <= 3751.05) {
			$percentagem_renda = 0.15;
		} else if ($salario >= 3751.06 && $salario <= 4664.68) {
			$percentagem_renda = 0.225;
		} else if ($salario >= 4664.68 ) {
			$percentagem_renda = 0.275;
		}

		function calcularImpostoRenda($salario, $percentagem_renda) {
			$valorImposto = $salario * $percentagem_renda;
			return $valorImposto;
		}

		

		echo "Com o salário: $salario";
		echo '<br />';
		echo "A percentagem de imposto é: $percentagem_renda";
		echo '<br />';
		echo "O valor a pagar de Impostos é: " . calcularImpostoRenda($salario, $percentagem_renda);*/

/* ------------------------------ OUTRA VERSAO ------------------------------ */

		function calcularIRPF($salario) {
			$imposto = 0;

			if($salario <= 1903.98) {
			$imposto = 0;

		} else if ($salario >= 1903.99 && $salario <= 2826.65) {
			$imposto = ($salario * 7.5)/100;

		} else if ($salario >= 2826.66 && $salario <= 3751.05) {
			$imposto = ($salario * 15)/100;

		} else if ($salario >= 3751.06 && $salario <= 4664.68) {
			$imposto = ($salario * 22.5)/100;

		} else if ($salario >= 4664.68 ) {
			$imposto = ($salario * 27.5)/100;

		}
			return $imposto;

		}

		echo calcularIRPF(5000);

		?>

		


	</body>
</html>


	</body>
</html>
